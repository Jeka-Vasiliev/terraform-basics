### 📋 Задание: "Хранилище с доступом"

Тебе нужно описать конфигурацию для **LocalStack**, которая включает:

1. **S3 Bucket** 🪣:
* Имя должно формироваться с использованием переменной `project_name` (например, `my-cool-project-data`).
* Добавь тег `Environment`, значение которого берется из переменной (по умолчанию `dev`).


2. **Security Group** 🛡️:
* Назови её `app-sg`.
* Используй **`dynamic` блок** для входящего трафика (`ingress`).
* Нужно открыть два порта: **80** (HTTP) и **443** (HTTPS).
* Данные для портов (номер и описание) должны храниться в одной переменной типа `map`.


3. **Lifecycle** 🔄:
* Добавь в Security Group правило `create_before_destroy = true`.

---

### 🛠️ Что нужно сделать:

1. Напиши `provider.tf` (настройку провайдера для LocalStack мы обсуждали выше).
2. Напиши `variables.tf` с необходимыми переменными.
3. Напиши `main.tf` с ресурсами.
4. Запусти `terraform init` и `terraform plan`.

**Когда будешь готов, присылай код и текст, который выдал `terraform plan`.**
