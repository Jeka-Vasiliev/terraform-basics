services:
  localstack:
    image: localstack/localstack:latest
    ports:
      - "4566:4566"      # LocalStack Gateway
    environment:
      - SERVICES=s3,ec2,iam,dynamodb,sqs,sns,lambda,rds,apigateway
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - AWS_DEFAULT_REGION=us-east-1
